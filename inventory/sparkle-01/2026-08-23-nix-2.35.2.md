# Nix 2.35.2 activation checkpoint

Activated on the `sparkle-01` pilot between 2026-08-23 22:45 and 22:46 +04
after Armen's explicit approval. This sanitized record contains no network
identifiers, credentials, environment variables, or complete logs.

## Scope and provenance

- Changed the root default Nix profile, reloaded the systemd manager, and
  restarted only `nix-daemon.service`.
- Used the repository's exact `root/nix/store-paths.nix`; the default
  Nixpkgs fallback remained blocked at older 2.34.8.
- Re-verified final tag 2.35.2, ARM64 artifact SHA-256, exact store path and NAR
  hash, signed `cache.nixos.org-1` metadata, and the custom dry run.
- Fetched and verified the signed closure before profile activation.
- Did not change DGX OS, NVIDIA packages, Home Manager, desktops, Tailscale,
  Docker, applications, or workloads.

## Before and after

| Item | Before | After |
| --- | --- | --- |
| Client/store protocol | Nix 2.35.1 | Nix 2.35.2 |
| Default profile target | `qing8va3…-user-environment` through the installer-created root-user profile | `9lznxxcs…-user-environment` through `default-1-link` |
| Exact Nix package | 2.35.1 in the retained environment | `fw98swa1…-nix-2.35.2` |
| Nix daemon | Active on 2.35.1 | Restarted cleanly; active on 2.35.2; no daemon reload pending |
| Tailscale | Apt-owned 1.102.3, active with Tailscale SSH | Same unit, process, start time, backend/online state, and SSH connectivity |
| GDM | Factory display manager active | Same process and start time; no restart; prior daemon-reload warning cleared |

The installer-created root-user profile still points to
`/nix/store/qing8va3nif2ydr6mw3pw41i58g11m5w-user-environment` and remains a
GC root. It contains Nix 2.35.1. It is a separate rollback anchor, not a prior
generation in the new `/nix/var/nix/profiles/default` lineage.

## Acceptance evidence

- PATH and default-profile clients report 2.35.2.
- The daemon store reports protocol version 2.35.2.
- `nix doctor` passes single-version PATH, GC-root, and protocol checks.
- A real repository policy derivation built through the restarted daemon.
- `./scripts/check.sh` passed.
- The online update audit reports upstream/repository Nix 2.35.2 current.
- A fresh Tailscale SSH login succeeded after the Nix daemon restart.

The exact rollback command and compatibility warning live in
[`root/nix/README.md`](../../root/nix/README.md). Do not garbage-collect the
retained environment until the pilot is accepted.
