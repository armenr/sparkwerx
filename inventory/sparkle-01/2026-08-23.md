# `sparkle-01` sanitized baseline

Captured read-only at `2026-08-23T09:33:58+04:00`. No serial numbers, network
identifiers, credentials, environment variables, or complete logs were
recorded.

## Platform

| Item | Baseline |
| --- | --- |
| Host role | Pilot DGX Spark |
| Architecture | `aarch64` / Debian `arm64` |
| OS | Ubuntu 24.04.4 LTS (Noble), DGX OS substrate |
| Kernel | `6.17.0-1031-nvidia` |
| CPU | 20 ARM64 cores: Cortex-X925 and Cortex-A725 |
| Memory | 121 GiB visible; 15 GiB swap |
| Root filesystem | ext4 on NVMe; 3.7 TiB total, 42 GiB used |
| Secure Boot | Enabled |

## NVIDIA and desktop

| Item | Baseline |
| --- | --- |
| GPU | NVIDIA GB10 |
| Driver | 580.173.02 |
| Reported CUDA compatibility | 13.0 |
| Display manager | GDM 46.2, active |
| Desktop base | `ubuntu-desktop` 1.539.2 |

`nvidia-smi` correctly reports memory usage as unsupported for the GB10 unified
memory design. GDM was active, though systemd reported that its unit files had
changed on disk since the manager was last reloaded. No reload was performed.

## Factory/runtime software retained

| Item | Baseline |
| --- | --- |
| Docker Engine | 29.2.1; service enabled and active |
| containerd.io | 2.2.1 |
| NVIDIA Container Toolkit | 1.20.0 |
| SSH service | Disabled and inactive |
| Tailscale | `1.102.3` official apt package for ARM64; daemon enabled and active |
| Tailscale SSH | Enabled; backend running and self online (identifiers omitted) |

Tailscale is not factory substrate. Its apt package is retained as working
migration input until a non-downgrading Nix package and root service are
reviewed, built, and safely activated. The node identity and all network/tailnet
identifiers were deliberately excluded.

## User-installed software

| Item | Baseline |
| --- | --- |
| ChatGPT Debian package | `chatgpt` 26.818.41705, ARM64 |
| Codex CLI | 0.149.0 standalone ARM64 build under `~/.codex` |
| Firefox extension | 1Password extension, manually installed (user-reported) |

## Pre-existing Nix installation

Nix existed before this repository was created.

| Item | Baseline |
| --- | --- |
| Nix version | 2.35.1 |
| Installer | `NixOS/nix-installer` 2.35.1 |
| Install time | 2026-08-22 21:01 local time |
| Mode | systemd daemon and socket, active/linked |
| Store sandboxing | Enabled |
| Binary cache | `https://cache.nixos.org/` only |
| Trusted users | `root` only |
| Build users | `nixbld1` through `nixbld32` present |
| Experimental features | `nix-command`; flakes not enabled persistently |
| Root profile | Nix 2.35.1 and static CA certificates only |
| User profile | None found for `n0b0dy` |

The installer receipt says `enable_flakes = false` and `add_channel = false`.
The Nix daemon was healthy, though systemd reported that its unit/socket files
had changed on disk since the manager was last reloaded. No configuration or
service state was changed during capture.
