# Reviewed aarch64-linux candidate for `nix upgrade-nix
# --nix-store-paths-url`; provenance lives in release.json. This replaces the
# stale default fallback pointer for this one audited update. It does not cause
# an update merely by existing in the repository.
{
  aarch64-linux = "/nix/store/fw98swa1g4ysvmv6p6m6xf51kzhvpp6p-nix-2.35.2";
}
