minimum_pre_commit_version: "4.6.2"
repos:
  - repo: local
    hooks:
      - id: sparkwerx
        name: Sparkwerx checks
        entry: ./scripts/dev lint
        language: unsupported
        pass_filenames: false
        always_run: true
