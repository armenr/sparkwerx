{
  # Separate from the retained System Manager generation. This selects an
  # opt-in boot adapter, not a driver, desktop session, or automatic activation.
  kms = {
    enable = true;
    menuSeconds = 5;
  };
}
